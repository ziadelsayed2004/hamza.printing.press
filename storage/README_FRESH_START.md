# Fresh database start

`database.sqlite` was intentionally removed from this delivered copy. Run:

```bash
npm run db:reset
```

after step 041 updates the seed/reset behavior, or let the agent execute step 041 first.
